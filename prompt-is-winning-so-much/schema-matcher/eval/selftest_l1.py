#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""自检：把 examples.md 里已通过执行验证的 3 轮回复喂给 l1_check_turn，必须全过。
这是 prompt-iter 的硬要求：parser 必须对着 ground-truth 校验，否则静默数错关键指标。"""
import sys
sys.path.insert(0, "eval")
from run import l1_check_turn

T1 = """<think_schema>
1. 抽象↔具体：偏抽象·微｜依据：问句形态"是怎么回事／想了解下"属理解导向，未要求操作步骤或实例
2. 中心↔边界：中心·锚点｜依据：常规科普场景，无例外、极端情况信号
3. 融贯↔符合：偏融贯·微｜依据：概念解释任务，需建立概念间联系与类比；无具体数字与时效核查要求
4. 精确↔召回：居中·锚点｜依据：无枚举、排查信号，入门理解以核心概念为先
5. 压缩↔展开：偏展开·微｜依据："想了解下"是要理解而非要决策，需适度展开概念结构
6. 构成↔调节：构成·锚点｜依据：微服务是定义稳定的技术概念，可作确定性断言
分层匹配：无
搁置：无
自检：正文详略、覆盖、模态与上述判断一致（生成后核对，不符则修正正文）
</think_schema>

微服务架构的核心想法是：把一个庞大的应用拆成一组小而独立的服务，每个服务只负责一块明确的业务（比如用户、订单、支付），各自独立开发、部署、扩容，服务之间通过网络接口协作。
"""

T2 = """<think_schema>
1. 抽象↔具体：沿用｜依据：同第1轮
2. 中心↔边界：沿用｜依据：同第1轮
3. 融贯↔符合：沿用｜依据：同第1轮
4. 精确↔召回：沿用｜依据：同第1轮
5. 压缩↔展开：偏压缩·重｜依据：用户明示"太长了，说重点"，属展开端错配信号，向压缩端重判
6. 构成↔调节：沿用｜依据：同第1轮
分层匹配：无
搁置：类比、优缺点展开、适用条件讨论｜用户追问细节或示例时启用
自检：正文压缩为结论句集合，与偏压缩·重判断一致
</think_schema>

- 微服务 = 把一个大应用拆成多个独立小服务，各自部署、靠接口通信。
- 好处：灵活、可独立扩容、团队边界清晰。
"""

T3 = """<think_schema>
1. 抽象↔具体：偏具体·微｜依据：问"什么情况不适合"指向可识别的具体情形与区分标准
2. 中心↔边界：偏边界·重｜依据：用户显式追问"不适合"的情形，即失效条件与例外，属中心端错配修正信号
3. 融贯↔符合：沿用｜依据：同第1轮
4. 精确↔召回：偏召回·微｜依据：枚举不适用场景，遗漏会让用户在错误场景误用，危害大于多列一条
5. 压缩↔展开：沿用｜依据：同第2轮；用户已确认的压缩偏好不推翻，仅保留必要区分标准
6. 构成↔调节：构成·锚点｜依据：适用性判断有稳定经验标准，可给明确判定
分层匹配：无
搁置：各情形的深入案例与展开论证｜用户追问其中某一条时启用
自检：正文以边界情形为主、逐条压缩，与判断一致
</think_schema>

有，以下情况通常不适合：
- 系统小、业务简单：单体就够用，拆分纯属自增复杂度。
- 初创期、需求未定型：服务边界会反复推翻，返工成本极高。
"""

# 一条故意带 bug 的，确认检查器能抓到
BAD = """<think_schema>
1. 抽象↔具体：微偏抽象｜依据：理解导向
2. 中心↔边界：居中·锚点｜依据：无
3. 融贯↔符合：偏符合·中｜依据：事实
4. 精确↔召回：沿用｜依据：同第1轮
5. 压缩↔展开：偏压缩较重｜依据：用户要求
6. 构成↔调节：构成·锚点｜依据：稳定
分层匹配：无
搁置：无
自检：一致
</think_schema>

这个图式匹配的错配处置表明，你应该向锚点移动。
"""

for i, (name, t, expect_pass) in enumerate(
    [("T1(open)", T1, True), ("T2(compress)", T2, True), ("T3(boundary)", T3, True),
     ("BAD(drift)", BAD, False)], start=1):
    r = l1_check_turn(t, forbidden_terms=True)
    status = "PASS" if r["pass"] == expect_pass else "*** MISMATCH ***"
    print(f"[{status}] {name}: pass={r['pass']} fails={r['fails']}")
