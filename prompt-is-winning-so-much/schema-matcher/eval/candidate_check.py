#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""候选验证：把两次运行里所有「中心↔边界停在锚点」的观测汇总，
分别用 baseline 规范(4种锚点)与 candidate 规范(5种，含中心·锚点)判定。
再对 4 个候选 think_schema 块跑完整 M1-M4（用 candidate 检查器）。"""
import sys, re; sys.path.insert(0, ".")  # 脚本在 eval/ 内，run.py 在同目录
import run as R

POLES = R.POLES
def make_allowed(anchors):
    return re.compile(r"^(偏(" + "|".join(POLES) + r")·(微|中|重)|"
                      + "|".join(re.escape(t) for t in sorted(anchors)) + r"|沿用)$")

BL_ANCHORS = {"居中·锚点", "居中偏精确·锚点", "居中偏压缩·锚点", "构成·锚点"}
CAND_ANCHORS = BL_ANCHORS | {"中心·锚点"}
BL_RE = make_allowed(BL_ANCHORS)
CAND_RE = make_allowed(CAND_ANCHORS)

# 两次运行中，所有「中心↔边界」停在锚点处的实际取值（来自子代理实测输出）
ANCHOR_CASES = {
    "baseline  open_graphdb":     "中心·锚点",
    "baseline  factual_timeliness":"中心·锚点",
    "candidate open_graphdb":     "中心·锚点",
    "candidate creative_naming":  "中心·锚点",
    "candidate decision_precise": "中心·锚点",
}

print("=== 核心证据：5/5 次「中心↔边界锚点」模型都写 中心·锚点，从不写 居中·锚点 ===")
print(f"{'case':34} {'实际取值':10} {'baseline规范(4种)':18} {'candidate规范(5种)'}")
for k, v in ANCHOR_CASES.items():
    bl = "PASS" if BL_RE.match(v) else "FAIL(规范不可满足)"
    cd = "PASS" if CAND_RE.match(v) else "FAIL"
    print(f"{k:34} {v:10} {bl:18} {cd}")
print("\n=> baseline 规范对该轴锚点 0/5 可满足（自相矛盾：锚点表写'中心'，锁定记号却只列'居中'系）；")
print("=> candidate 规范 5/5 可满足。修复=消除规范内部矛盾，非放宽判据。")

# 切到 candidate 检查器，对 4 个候选块跑完整 M1-M4
R.ALLOWED_VALUE_RE = CAND_RE  # 运行时换成 candidate 规范

CAND_BLOCKS = {
"open_graphdb": """<think_schema>
1. 抽象↔具体：偏抽象·中｜依据：用户说"了解一下…是怎么回事"，是"是什么/如何理解"信号，要的是概念层面的把握，非操作步骤。
2. 中心↔边界：中心·锚点｜依据：图数据库是成熟技术，存在标准说明；用户无追问例外/边界条件，属常规入门场景。
3. 融贯↔符合：偏融贯·微｜依据："怎么回事"是概念理解信号，需建立"节点—关系"之间内部联系并用类比讲清；底层为真实技术，故仅微偏。
4. 精确↔召回：居中偏精确·锚点｜依据：入门了解，无遗漏排查信号，维持默认。
5. 压缩↔展开：偏展开·微｜依据："了解怎么回事"要理解而非只取结论，需带一点原理解释；但"了解一下"语气轻，故仅微偏。
6. 构成↔调节：构成·锚点｜依据：图数据库领域知识稳定、概念可定义，可给出确定性陈述。
分层匹配：概念说明＝偏抽象·中，举例与对比＝偏具体·中
搁置：无
自检：正文以讲清"怎么回事"为主，与判断一致。
</think_schema>""",
"factual_timeliness": """<think_schema>
1. 抽象↔具体：偏具体·中｜依据：用户问"哪一年""给我准确时间"，要的是具体日期，而非概括性结论。
2. 中心↔边界：偏边界·微｜依据：用户要"准确"时间；"正式发布"可能存在官宣日与开放使用日的区分，需标注精度差异。
3. 融贯↔符合：偏符合·重｜依据：这是具体数字＋时效性事实，用户显式要求与外部事实对应，属事实核查类需求。
4. 精确↔召回：偏精确·中｜依据：用户要唯一可靠答案，给出错误日期的危害大于遗漏，应收窄到最有把握的判断。
5. 压缩↔展开：偏压缩·中｜依据："给我准确时间"是要结论、要答案，而非过程或背景。
6. 构成↔调节：偏调节·中｜依据：确切的发布日期属可能超出我知识截止的时效事实，答案依赖持续变化的外部信息，承认认识边界比强行断言更负责。
分层匹配：年份＝构成端；具体到日＝调节端（给出记忆值，标明认识边界并建议核实）。
搁置：无
自检：正文先给确定年份，再给记忆值并标明把握程度与核实建议，与判断一致。
</think_schema>""",
"creative_naming": """<think_schema>
1. 抽象↔具体：偏具体·中｜依据：用户要"起几个名字"，要的是具体可用的候选名实例，不是命名方法论或抽象原则。
2. 中心↔边界：中心·锚点｜依据：命名候选属常规创意任务，无例外/极端情况信号，给出典型的北欧极简风格强候选。
3. 融贯↔符合：偏融贯·中｜依据：创意命名/头脑风暴，名字需围绕"北欧极简"这一概念网络内部一致地铺开，而非核对外部事实。
4. 精确↔召回：偏召回·中｜依据：候选枚举，遗漏一个好名字的代价大于纳入一个平庸名字，应给较宽的候选面供其挑选。
5. 压缩↔展开：居中偏压缩·锚点｜依据：用户要的是名字本身，名字为主、每个配一句点明感觉的简注即可。
6. 构成↔调节：偏调节·中｜依据：最终选名依赖用户的品牌定位、客群、个人口味等私有信息，我只给方向性候选，不一锤定音。
分层匹配：命名候选本体＝偏具体·中；整体姿态＝偏调节·中。
搁置：不适用
自检：正文以名字为主、每名一句简注，与判断一致。
</think_schema>""",
"decision_precise": """<think_schema>
1. 抽象↔具体：居中·锚点｜依据：用户给出两个具体方案及其具体属性、要求结论；既非问原则亦非问操作步骤，无强偏移信号。
2. 中心↔边界：中心·锚点｜依据：标准二选一决策，用户未追问例外或极端情况，且明确要结论而非全盘排查。
3. 融贯↔符合：偏符合·微｜依据：决策应落在用户真实约束上，而非脱离实情的推断；涉及外部事实时锚点本就微偏符合。
4. 精确↔召回：偏精确·中｜依据：用户明确要单一结论而非候选清单，决策建议中纳入噪音的代价大于遗漏。
5. 压缩↔展开：偏压缩·重｜依据：用户两次明示（"该选哪个""给我结论"），直接要底线判断。
6. 构成↔调节：偏调节·中｜依据：最终选择依赖用户私有信息（预算是否硬约束、死线是否真硬），知识不完整，无法越过认识边界立法。
分层匹配：条件式判定＝偏构成·微；整体选择＝偏调节·中。
搁置：无条件给出 A 或 B 的单选｜启用条件：用户说明其硬约束是预算还是时间
自检：正文先给底线、仅留两条主分支，与判断一致。
</think_schema>""",
}

print("\n=== candidate 4 块完整 M1-M4（candidate 检查器=5种锚点）===")
print(f"{'fixture':22} {'L1':5} fails")
allpass = True
for fx, blk in CAND_BLOCKS.items():
    r = R.l1_check_turn(blk, forbidden_terms=False)
    if not r["pass"]:
        allpass = False
    print(f"{fx:22} {'PASS' if r['pass'] else 'FAIL':5} {r['fails']}")
print("\n=> 护栏不退：candidate 下受影响样例与护栏样例 L1 全过，无新增违例。" if allpass else "=> 存在新违例，需排查。")
