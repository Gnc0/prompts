{
  "_doc": "endpoint/key 从环境变量读，绝不进本文件。运行前 export SCHEMA_EVAL_BASE_URL 与 SCHEMA_EVAL_API_KEY（或复用 OPENAI_BASE_URL/OPENAI_API_KEY）。configs 里只放 model 名、用哪个 variant、重复次数 reps。judge 是 L2 判官模型（可空，留空则只跑 L1 确定性层）。",

  "configs": [
    { "name": "baseline-gpt5",     "model": "REPLACE_WITH_MODEL_ID", "variant": "baseline", "reps": 5 },
    { "name": "baseline-deepseek", "model": "REPLACE_WITH_MODEL_ID", "variant": "baseline", "reps": 5 }
  ],

  "judge": {
    "model": "REPLACE_WITH_JUDGE_MODEL_ID",
    "_doc": "判官单次 sd≈2，故 --judge-k 默认取 5 次中位数（关键对比 ≥5）。"
  },

  "fixtures_file": "fixtures.json",
  "variants_dir": "variants",

  "forbidden_term_variants": [
    "微偏", "较重", "稍微", "略微", "偏微", "重偏", "中等偏移", "略微偏"
  ]
}
